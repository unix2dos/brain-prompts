<!-- ⚠️ 此文件由 sync_alfred_prompts.py 自动生成，请勿手动编辑 -->
<!-- 修改请编辑 Alfred snippets，然后运行脚本重新生成 -->

# Alfred AI Prompt - Core

### 001_深度追问

使用本机 $grill-with-docs 的 skill 开始。

### 002_判断是否澄清

使用 $ask-first  判断是否需要澄清；如果目标和约束已经足够明确，直接继续。

### 003_12岁男孩听懂

使用 $ljg-plain 解释，在当前回复中使用 Markdown 格式。

### 004_下一步

简要描述下之前都做了什么，结合当前进展总结一下。如果还有未完成或值得继续的工作，给出下一步建议及理由。

### 005_提交代码前检查

检查当前工作区是否有属于本次任务的未提交改动。列出相关文件、建议提交范围和验证状态；不要提交、推送或直接操作 main，除非我明确要求。

### 006_睡觉执行

我现在要睡觉，接下来无法及时回复。请把上文已经明确、属于当前任务且可以安全在本地完成的工作尽量推进到结束，包括编辑和验证。对于可逆且不会改变整体方向的小歧义，按合理假设继续并记录假设；遇到必须由我决定、外部写入、破坏性操作、付费行为或明显扩大范围的事项，先跳过并继续其他独立工作，最后集中列出。完成前运行相关检查并报告结果。只有任务确实需要跨轮跟踪时才创建进度文件。

### 007_冲突解决

解决当前本地 merge 或 rebase 冲突。先判断冲突两侧的意图；能从代码、历史和测试确定的直接解决，只有涉及业务语义且无法判断时才询问我。不要中止当前 merge/rebase。完成后运行相关验证并报告结果。

### 009_有钱人思考

用富人思维重新审视上文：关注长期价值、所有权、杠杆、复利、机会成本、非对称收益、最大下行风险和选择权。必要时借鉴马斯克的第一性原理、贝索斯的长期主义与可逆决策、纳瓦尔的特定知识与所有权，但不要模仿人物、猜测他们本人会怎么做或输出成功学口号。结合我的现实约束，指出当前思路中限制财富增长的默认假设，并给出更高杠杆的替代思路。若这个视角不适用于当前问题，直接说明。

### 010_项目代码地图讲解

请用中文为当前代码建立一张由整体到局部的地图。若上文指定了文件、符号或模块，聚焦它在系统中的位置；否则分析整个项目。说明项目或组件的目标、主要入口、核心模块、依赖与调用关系、关键数据流、外部系统及失败边界，并解释关键实现的设计意图。结论引用实际文件或符号作为依据，区分代码事实与合理推断，优先使用项目已有的领域术语；不要逐文件罗列，不做代码审查，不修改代码。分析整个项目时给出建议阅读顺序；只有关系复杂且图形能明显提升理解时才使用 Mermaid。

### 011_代码审查

重点检查正确性、安全性、可靠性、性能、可维护性，以及是否存在绕路设计或不必要的复杂度。只报告有代码证据且值得行动的问题；每项给出严重程度、位置、触发条件、影响、判断依据和具体改进方向。区分事实与推断，不为凑数而报告问题。审查范围较大时说明实际覆盖范围和未检查区域；没有发现问题时明确说明，并指出尚未覆盖或无法验证的部分。只审查，不修改代码。

### 012_向下追本

使用 $ljg-think 纵向深挖上文，追到根本假设或不可再分的原理，在当前回复中使用 Markdown 格式。

### 013_向上抽象

我不熟悉上文涉及的领域。请上移一个抽象层级，使用该领域已有的术语，给出相关组成部分、主要参与者及其关系的地图。
